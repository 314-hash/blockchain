
export interface Character {
    id: string;
    name: string;
    image: string; // base64 or URL
}

export interface Scene {
    id: string;
    name: string;
    image: string; // base64 or URL
}
